CDECK  ID>, NLINK.
      INTEGER FUNCTION NLINK(NAME,NR)
      IMPLICIT NONE
#include "bcs.inc"
      INTEGER NR
      CHARACTER*4 NAME
      INTEGER JW(1000)
      EQUIVALENCE (IW(1),JW(1))
      INTEGER MLINK
      NLINK=MLINK(JW,NAME,NR)
      RETURN
      END
