CDECK  ID>, BKFMT.
      SUBROUTINE BKFMT(NAME,FMT)
      IMPLICIT NONE
#include "bcs.inc"
      CHARACTER*4 NAME
      CHARACTER*(*) FMT
      INTEGER JW(1000)
      EQUIVALENCE (IW(1),JW(1))
      CALL MKFMT(JW,NAME,FMT)
      RETURN
      END
