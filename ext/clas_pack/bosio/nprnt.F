CDECK  ID>, NPRNT.
      INTEGER FUNCTION NPRNT(NAME,NR)
c PRINT BANK (NAME,NR) AND RETURN INDEX
      IMPLICIT NONE
#include "bcs.inc"
      CHARACTER*4 NAME
      INTEGER NR
      INTEGER JW(1000)
      EQUIVALENCE (IW(1),JW(1))
      INTEGER MPRNT
      NPRNT=MPRNT(JW,NAME,NR)
      RETURN
      END
