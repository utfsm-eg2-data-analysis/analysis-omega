CDECK  ID>, NDROP.
      INTEGER FUNCTION NDROP(NAME,NR)
      IMPLICIT NONE
#include "bcs.inc"
      CHARACTER*4 NAME
      INTEGER NR
      INTEGER JW(1000)
      EQUIVALENCE (IW(1),JW(1))
      INTEGER MDROP
      NDROP=MDROP(JW,NAME,NR)
      RETURN
      END
